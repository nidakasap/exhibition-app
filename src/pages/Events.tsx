import Navbar from "../components/Navbar"

const Events = () => {
  return (
    <div className="h-[100vh] bg-[radial-gradient(ellipse_at_center,_var(--tw-gradient-stops))] from-indigo-600 via-cyan-400 to-blue-600">
      <Navbar/>
    </div>
  )
}

export default Events